@model DownloadsViewModel

@{
    ViewData["Title"] = "Download";
}

<h1>Available installs</h1>

@{ 
    for(int i = 0; i < Model.Files.Length; i++)
    {
        <a href="~/downloads/@Model.Files[i].Name" download>@Model.Files[i].Name</a>
    }
}